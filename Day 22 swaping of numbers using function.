#include<stdio.h>

int swap(int a, int b);

int main(){

int a,b;

printf("Enter numbers: ");

scanf("%d %d",&a,&b);

swap(a,b);

return 0;

}

int swap(int a, int b){

    a=a+b;

    b=a-b;

    a=a-b;

    printf("a=%d, b= %d",a,b);

}
